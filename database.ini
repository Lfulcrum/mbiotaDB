% TEMPLATE FOR DB CONFIGURATION FILE
% Replace <FIELD> with the appropriate data. 

[postgresql]
host=<DB_HOST>  % For a local database, use: localhost
database=<DB_TO_CONNECT>
user=<DB_USERNAME>
password=<DB_USER_PASSWORD>

[sqlalchemy postgresql]
host=<DB_HOST>  % For a local database, use: localhost
database=<DB_TO_CONNECT>
username=<DB_USERNAME>
password=<DB_USER_PASSWORD>

[qiita]
username=<QIITA_USERNAME>
password=<QIITA_PASSWORD>